{
  "projectName": "Automated Course Change Logs",
  "description": "An automated system to track weekly changes in Canvas courses by downloading IMSCC exports, comparing them against previous versions, and emailing a markdown diff report to stakeholders.",
  "appArchitecture": {
    "modules": [
      {
        "name": "Controller (main.py)",
        "role": "Orchestrator",
        "description": "Reads the configuration, delegates tasks to workers sequentially, and handles overall error logging and task flow."
      },
      {
        "name": "Configuration Manager (config_loader.py)",
        "role": "Utility",
        "description": "Loads the JSON database of Canvas course links, email lists, and app settings."
      },
      {
        "name": "Canvas Scraper (scraper.py)",
        "role": "Worker",
        "description": "Uses Playwright or Selenium to authenticate to Canvas, navigate to course settings, trigger content export, and download the IMSCC file to a designated staging directory."
      },
      {
        "name": "IMSCC Processor (imscc_handler.py)",
        "role": "Worker",
        "description": "Extracts the downloaded IMSCC (ZIP format) files into course-specific and timestamp-specific directories."
      },
      {
        "name": "Diff Generator (differ.py)",
        "role": "Worker",
        "description": "Compares the current week's extracted files against the previous week's files. Generates a formatted markdown report highlighting additions, deletions, and modifications."
      },
      {
        "name": "Email Notifier (emailer.py)",
        "role": "Worker",
        "description": "Reads the generated markdown diff and emails it to the configured list of shareholders using SMTP or an email API."
      },
      {
        "name": "Task Scheduler Setup (scheduler_setup.ps1)",
        "role": "Infrastructure",
        "description": "A PowerShell script to configure Windows Task Scheduler to run the Controller script upon the first user logon of the week."
      }
    ],
    "fileSystemStructure": {
      "root": "./",
      "src": "./src/",
      "data": "./data/courses.json",
      "exports": "./exports/{course_id}/{year_week}/",
      "reports": "./reports/"
    }
  },
  "agentInstructions": [
    "Do not use any AI integrations for text generation or analysis. Use pure algorithmic diffs.",
    "Ensure strict modularity. Each worker must have clear input and output interfaces and should not depend tightly on other workers.",
    "Use robust error handling for the web scraping module, as Canvas UI might be slow or change slightly.",
    "The IMSCC (which is a ZIP format) extraction should handle large files safely.",
    "The diff generation should compare file hashes and structures. For text-based files (XML/HTML), generate line-by-line diffs if modified.",
    "All configuration (URLs, emails, credentials) must be read from environment variables or a secure local configuration file."
  ],
  "expectedBehaviors": {
    "trigger": "Executes on Windows Logon. The main script includes wrapper logic that ensures the heavy lifting only runs if it hasn't run successfully yet for the current calendar week.",
    "processing": "Iterates through all courses sequentially, downloading and unzipping their IMSCC.",
    "comparison": "Finds the immediate previous export folder for each course. If none exists, treats all files as 'new'. Otherwise, runs a recursive directory diff.",
    "reporting": "Consolidates all course diffs into a single Markdown document.",
    "delivery": "Sends the markdown document (and optionally renders it to HTML for the email body) to the shareholder list."
  }
}
